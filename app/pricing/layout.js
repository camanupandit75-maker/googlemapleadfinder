export const metadata = {
  description:
    "Geonayan credits start at ₹499. Pay per search, no subscription. 10 free searches on signup.",
};

export default function PricingLayout({ children }) {
  return children;
}

